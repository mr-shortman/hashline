# Optional: copy to .env.desktop.local. Values are literal, without shell expansion.
# Existing environment variables take precedence. Normal installations need no file.
# CARGO_HOME=/absolute/path/to/cargo
# RUSTUP_HOME=/absolute/path/to/rustup
# HASHLINE_DEV_SYSROOT=/absolute/path/to/sysroot
